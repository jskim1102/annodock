export const RTX_3090_TRAINING_DEFAULTS = {
  exclude_unlabeled_images: false,
  include_unlabeled_images_in_test: false,
  epochs: 200,
  imgsz: 640,
  batch: 32,
  device: 0,
  optimizer: "auto",
  lr0: 0.01,
  lrf: 0.01,
  warmup_epochs: 3,
  cos_lr: true,
  patience: 30,
  augment: true,
  mosaic: 1,
  mixup: 0,
  copy_paste: 0,
  close_mosaic: 10,
  hsv_h: 0.015,
  hsv_s: 0.7,
  hsv_v: 0.4,
  fliplr: 0.5,
  scale: 0.5,
  translate: 0.1,
  workers: 8,
  cache: "ram",
  amp: true,
  compile: true,
  deterministic: false,
  save_period: 25,
  multi_scale: 0,
} as const;
